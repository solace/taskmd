Task **042** was not found in the project.

No exact match exists for task ID "042". The closest partial match suggested by the CLI was:

- **142**: Detect new pending task files in commit-msg and adjust message (67% match)

To find available tasks, you can run:

```bash
taskmd list
```

This will display all tasks in the project with their IDs, titles, statuses, and priorities.

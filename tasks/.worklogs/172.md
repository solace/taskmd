## 2026-02-20T10:00:00Z

Starting documentation task for missing web features, API endpoints, and config options.

**Approach:** Will update three documentation files:
1. `apps/docs/guide/web.md` — add missing pages, features, and full API section
2. `docs/guides/web-guide.md` — mirror the same additions
3. `apps/docs/reference/configuration.md` — add 4 missing config options

**Research completed:** Explored source code for all API handlers, web components, and config parsing. Have accurate details for all items.

## 2026-02-20T10:30:00Z

Task completed. All documentation updates applied.

**Changes made:**

1. **`apps/docs/guide/web.md`:**
   - Added Task Detail View section with URL `/tasks/:id`
   - Added new "Web Features" section covering: task editing (edit form + drag-and-drop), static site export (`web export`), read-only mode (`--readonly`), board filters, and graph search/highlighting
   - Replaced minimal API section with comprehensive endpoint tables (14 endpoints), examples, and PUT request documentation

2. **`docs/guides/web-guide.md`:**
   - Added Next (Recommendations), Tracks, Validate, and Task Detail views (sections 5-8)
   - Added navigation entries for Next, Tracks, Validate
   - Added "Web Features" section (task editing, static export, readonly, board filters, graph search)
   - Replaced minimal API section with full endpoint tables, examples, and PUT documentation
   - Updated "What You'll Learn" section and "Export and Sharing" subsection

3. **`apps/docs/reference/configuration.md`:**
   - Added `ignore`, `worklogs`, `workflow`, and `todos.exclude` to Supported Options table
   - Added detailed descriptions with examples for each new option

**Decisions:**
- Both web guides got the same content adapted to their respective structure (apps/docs uses concise format, docs/guides uses more detailed tutorial style)
- API endpoints documented as tables with method/path/description, plus curl examples
- PUT endpoint got its own sub-section with request body format and valid enum values

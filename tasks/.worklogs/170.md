## 2026-02-20T12:00:00Z

Started fixing documentation inaccuracies found in audit.

**Approach:** Read all four doc files and compared against actual CLI `--help` output. Confirmed:
- Global flag is `--task-dir` / `-d`, not `--dir`
- `set --status` accepts `in-review` in addition to existing values
- `set` has `--add-pr`, `--remove-pr`, `--type`, `--verify` flags missing from docs
- Web graph uses `@xyflow/react` (ReactFlow), confirmed no Mermaid in web app
- Config IS implemented (`.taskmd.yaml` exists and works)
- `--debug` and `--no-color` are real global flags

**Files to edit:**
- `apps/docs/guide/cli.md` - Global Flags, set command
- `docs/guides/cli-guide.md` - Global Flags, set command
- `apps/docs/guide/web.md` - Graph view description
- `docs/guides/web-guide.md` - Graph view, config section, future features

## 2026-02-20T12:15:00Z

Completed all documentation fixes.

**Changes made across 4 files:**

1. **`--dir` -> `--task-dir`**: Fixed in Global Flags sections of both CLI guides, web start examples in all four doc files, and shell alias examples
2. **`in-review` status**: Added to `set --status` valid values in both CLI guides
3. **Missing `set` flags**: Added `--add-pr`, `--remove-pr`, `--type`, `--verify` to flag tables in both CLI guides
4. **Config section**: Replaced stale "planned but not yet implemented" text with actual `.taskmd.yaml` config example in `docs/guides/web-guide.md`
5. **Graph view**: Changed "Mermaid diagrams" to "@xyflow/react (ReactFlow)" in both web guides; renamed "Mermaid rendering issues" to "Graph rendering issues"
6. **Future features**: Removed "Inline editing", "Drag and drop", and "Task creation" from future features list (already implemented)
7. **Global flags**: Added `--debug` and `--no-color` to both CLI guides' Global Flags sections

**Additional fixes found during work:**
- Fixed `--dir` in web start examples across all doc files (not just Global Flags sections)
- Fixed shell alias example using `--dir` in cli-guide.md

## 2026-02-22T12:00:00Z

Started working on publishing Docker image to GHCR.

**Findings:** Most of the infrastructure was already in place from task 183:
- Dockerfile already exists (multi-stage, non-root, tini)
- Release workflow already has a docker job pushing to ghcr.io/driangle/taskmd
- CI workflow already builds and tests the Docker image
- Docker docs already exist in README.md and docs site

**Changes made:**
- Added OCI labels to Dockerfile (title, description, url, source, license, version)
- Added `ARG VERSION=dev` to stage 3 so the version label resolves correctly
- Added QEMU setup step to release workflow for multi-platform builds (linux/amd64 + linux/arm64)
- Added "Set package to public" step using `gh api` to ensure GHCR package visibility
- Checked off all subtasks in the task file

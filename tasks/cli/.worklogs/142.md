## 2026-02-17T10:00:00Z

Started implementation of task 142: detect new pending task files in commit-msg.

**Approach:** Extend the existing diff parsing in `commit_msg.go` to detect newly added files with `+status: pending`. Add a `parseNewPendingFilesFromDiff` function mirroring the existing `parseCompletedFilesFromDiff`. Update `runCommitMsg` to fall back to new-pending detection when no completed tasks are found. Generate messages like `chore: added task <ID>` for single or `chore: added tasks <ID1>, <ID2>` for multiple.

**Key design decisions:**
- Completed tasks take priority over new pending tasks (existing behavior preserved)
- Reuse the same diff parsing pattern as `parseCompletedFilesFromDiff`
- The `--type` flag override applies to the new pending path as well
- New files are detected by `+status: pending` in the diff (same pattern as completed detection)

## 2026-02-17T10:15:00Z

Implementation complete. All tests pass (28 total, 11 new).

**Changes made to `commit_msg.go`:**
- Added `parseNewPendingFilesFromDiff()` — detects newly added files (`--- /dev/null`) with `+status: pending`
- Added `findNewPendingTasksFromDiff()` — mirrors `findCompletedTasksFromDiff` for pending tasks
- Added `buildAddedTaskMessage()` — generates `chore: added task <ID>` / `chore: added tasks <ID1>, <ID2>` messages
- Updated `runCommitMsg()` — falls back to new-pending detection when no completed tasks found

**Tests added to `commit_msg_test.go`:**
- `TestCommitMsg_SingleNewPendingTask` — single new pending file generates correct message
- `TestCommitMsg_MultipleNewPendingTasks` — multiple new pending files
- `TestCommitMsg_NewPendingWithTypeFlag` — `--type` flag override works
- `TestCommitMsg_CompletedTakesPriorityOverNewPending` — mixed scenario, completed wins
- `TestCommitMsg_ModifiedPendingNotDetected` — only truly new files (not modified) are detected
- `TestParseNewPendingFilesFromDiff` — 6 subtests for the diff parser
- `TestBuildAddedTaskMessage` — 3 subtests for the message builder

No regressions in existing tests. Linter passes cleanly.

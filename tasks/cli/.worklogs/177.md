## 2026-02-20T11:00:00Z

Implemented e2e tests for config loading and precedence.

**Approach:** Extended the e2e test infrastructure with `runWithHome` (to control HOME directory), `buildCmd`/`execCmd` (shared helpers), and `writeConfig` helper. Wrote 12 tests covering all acceptance criteria.

**Changes:**
- `apps/cli/internal/e2e/e2e_test.go`: Refactored `run` into `buildCmd` + `execCmd` helpers for reuse
- `apps/cli/internal/e2e/config_test.go`: New file with 12 e2e tests

**Tests written:**
- [x] `TestConfig_ProjectTaskDir` - project config `task-dir` key
- [x] `TestConfig_ProjectDirLegacy` - legacy `dir` key
- [x] `TestConfig_ProjectVerbose` - verbose config enables scanner logs
- [x] `TestConfig_HomeFallback` - home config used when no project config
- [x] `TestConfig_ProjectOverridesHome` - project config wins over home config
- [x] `TestConfig_CLIFlagOverridesProjectConfig` - `--task-dir` flag beats project config
- [x] `TestConfig_CLIFlagOverridesHomeConfig` - `--task-dir` flag beats home config
- [x] `TestConfig_VerboseFlagOverridesConfig` - `--verbose` flag works regardless of config
- [x] `TestConfig_NoConfigFile` - works with no config
- [x] `TestConfig_NoConfigFileNoError` - no errors when config missing
- [x] `TestConfig_WorkflowSetting` - workflow config loads without error
- [x] `TestConfig_IgnoreDirs` - ignore config skips directories
- [x] `TestConfig_ExplicitConfigFlag` - `--config` flag overrides everything

**Note:** The "Using config file:" message only prints when `--verbose` is passed as a CLI flag (not via config), because the Go variable `verbose` is checked at `initConfig` time before config values are read. Tests were adjusted to check for scanner verbose logs instead.

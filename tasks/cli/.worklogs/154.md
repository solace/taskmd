## 2026-02-19T10:00:00Z

Implemented positional task ID argument for the `set` command.

**Approach:** Added a `resolveSetTaskID` helper that checks positional arg first, then `--task-id` flag as fallback. Errors on conflict (different values) or missing (neither provided). Removed `MarkFlagRequired("task-id")` since the task ID can now come from either source.

**Changes:**
- `apps/cli/internal/cli/set.go`: Updated `Use` to `set [task-id]`, `Args` to `MaximumNArgs(1)`, added `resolveSetTaskID()`, updated help text/examples, removed required flag constraint
- `apps/cli/internal/cli/set_test.go`: Added `captureSetOutputWithArgs` helper plus 4 new tests (positional, same-value, conflict, neither-provided)

**Completed:**
- [x] All 5 subtasks
- [x] All 40 set command tests pass (36 existing + 4 new)
- [x] Linter clean
- [x] Full test suite passes

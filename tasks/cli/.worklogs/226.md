## 2026-02-26T00:00:00Z

Added "Effort" column to the `next` command's table output.

**Approach:** The `Recommendation` struct already had an `Effort` field (populated by the scoring logic). The only change needed was in `outputNextTable` to include it in the table header and row data, using the existing `formatEffort` color helper.

**Completed:**
- [x] Added "Effort" header between "Priority" and "File"
- [x] Added `rec.Effort` to both plain and colored row arrays
- [x] Used existing `formatEffort()` for color coding
- [x] Updated `TestNext_TableFormat` to assert "Effort" column header
- [x] All 30 next command tests pass

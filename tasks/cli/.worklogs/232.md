## 2026-02-28T00:00:00Z

Implemented `taskmd current` command.

**Completed:**
- [x] Created `internal/cli/current.go` — scans for in-progress tasks, outputs `#<ID> <title>` with 30-char truncation
- [x] Created `internal/cli/current_test.go` — 3 tests covering happy path, no task, and truncation
- [x] Simplified `~/.claude/statusline-command.sh` to use `$(taskmd current)` instead of jq/JSON parsing
- [x] Added docs in `apps/docs/guide/cli.md` with statusline integration examples (Claude Code, tmux, zsh, Starship)
- [x] Fixed pre-existing dead link in `apps/docs/reference/specification.md`

**Decisions:**
- Placed command after `next` in docs since they're related ("what should I work on" vs "what am I working on")
- Used `applyFilters` with `status=in-progress` to reuse existing filter infrastructure
- 30-char title truncation keeps statuslines compact

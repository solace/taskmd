## 2026-02-20T12:00:00Z

Implemented comprehensive e2e error handling and edge case tests.

**Approach:** Created `apps/cli/internal/e2e/error_test.go` following the established e2e test patterns (build tag, `run`/`mustRun` helpers, `setupTaskDir`). Tested actual CLI behavior by running the binary first to understand exact error messages and exit codes.

**Key findings during exploration:**
- `validate --stdin` is accepted (global flag) but validate still scans the directory, not stdin
- `list --status bogus` (via `--filter`) returns empty results without error (no enum validation in filter)
- `set --status bogus` validates enums strictly with helpful suggestions
- Empty directories return exit code 0 for list/next/graph/validate (graceful handling)
- Files without `id` or with empty `title` are silently skipped by the scanner (not validation errors)
- Exit codes: 0 (success), 1 (error), 2 (strict warnings only)

**Tests written (22 tests total):**
- Unknown command: exits non-zero, mentions command name, suggests alternatives for typos
- Missing required args: `set` with no ID, `set` with no update flags
- Non-existent tasks: `set` and `get` on missing task IDs
- Invalid flag values: invalid status on `set`, invalid format on `validate`/`list`
- Validate malformed files: invalid status, duplicate IDs, missing deps, circular deps, strict warnings
- Empty directory: list/next/graph/validate all succeed gracefully
- Invalid `--task-dir`: non-existent path handled without panic
- Exit codes: 0/1/2 verified for success/error/strict-warning
- Stderr quality: actionable messages, no stack traces
- Stdin/pipe: validate with `--stdin` and piped content don't crash

All 22 tests pass. No lint issues in new code.

## 2026-02-25T12:00:00Z

Implemented clickable status, priority, and effort values on the Stats page.

**Approach:** Followed the existing pattern from task 207 (clickable tags). Added a `linkParam` prop to `BreakdownCard` so each value renders as a `<Link>` to `/tasks?<param>=<value>`. On the Tasks page, read the new URL params and pass them to `TaskTable` as initial filter state.

**Changes:**
- `StatsView.tsx`: Added `linkParam` to `BreakdownCard`, rendering values as `<Link>` elements with blue hover styling (matching existing tag links)
- `TasksPage.tsx`: Read `status`, `priority`, `effort` URL params via `getAll()`, pass as props to `TaskTable`
- `TaskTable.tsx`: Accept new `initialStatuses`, `initialPriorities`, `initialEffort` props; initialize filter state from URL params when present; generalized `syncTagsToUrl` → `syncFiltersToUrl` to sync all filter params back to URL; added `selectedEffort` filtering; status/priority toggles in FilterBar now also sync to URL

**Completed:**
- [x] All five subtasks

## 2026-02-25T12:00:00Z

Implemented clickable tags on the Stats page.

**Approach:** Used React Router's `<Link>` component to wrap each tag name in the Tags section of `StatsView.tsx`. Each link navigates to `/tasks?tag=<tagName>` with proper URL encoding via `encodeURIComponent`. The List page already reads `?tag=` URL params via `useSearchParams().getAll("tag")`, so no changes needed there.

**Styling:** Tags now use blue text (`text-blue-600` / `dark:text-blue-400`) with hover underline and darkened color to clearly indicate interactivity.

**Completed:**
- [x] Wrapped tag names with `<Link to="/tasks?tag=...">` in StatsView.tsx
- [x] Navigation uses existing URL param filtering on Tasks page
- [x] Added hover styles (underline, color change, cursor pointer)

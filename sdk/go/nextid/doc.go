// Package nextid generates the next task ID from existing ID lists.
//
// It supports sequential, prefixed, random, and ULID formats while
// preserving common prefixes and zero-padding patterns.
package nextid
